language: python

python:
  - 2.7

install:
  - pip install -r requirements.txt

before_script:
 - chmod +x ./tests/all_tests.py

script:
  - ./tests/all_tests.py

after_failure:
  - cat ./tests/Logs/sickrage.log
